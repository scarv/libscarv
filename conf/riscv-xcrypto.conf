# Copyright (C) 2019 SCARV project <info@scarv.org>
#
# Use of this source code is restricted per the MIT license, a copy of which 
# can be found at https://opensource.org/licenses/MIT (or should be included 
# as LICENSE.txt within the associated archive or repository).

ifndef RISCV
  $(error "point RISCV environment variable at toolchain installation")
endif

# =============================================================================

export ARCH_SUBSET           = rv32imaxc
export ARCH_ABI              = ilp32

export TOOL_PREFIX_TARGET    = riscv32-
export TOOL_PREFIX_VENDOR    = unknown-
export TOOL_PREFIX_ABI       = elf-

export TOOL_PREFIX           = ${RISCV}/bin/${PREFIX_TARGET}${PREFIX_VENDOR}${PREFIX_ABI}

export TEST_PREFIX           = 
export TEST_SUFFIX           = 

export CC_PATHS              =
export CC_FLAGS              = -Wall -O3 -march=${ARCH_SUBSET} -mabi=${ARCH_ABI} 
export CC_LIBS               =

# =============================================================================

export CONF_AES_TYPE         = space

export CONF_AES_ENABLE_ENC   = 1
export CONF_AES_ENABLE_DEC   = 1

export CONF_AES_PRECOMP_SBOX = 1
export CONF_AES_PRECOMP_TBOX = 0
export CONF_AES_PRECOMP_MULX = 1
export CONF_AES_PRECOMP_DIVX = 1
export CONF_AES_PRECOMP_RC   = 1
export CONF_AES_PRECOMP_RK   = 1

export CONF_AES_KEY_FWD      = 1
export CONF_AES_KEY_REV      = 0

export CONF_AES_ROUND_SPLIT  = 1
export CONF_AES_ROUND_PACK   = 0
export CONF_AES_ROUND_UNROLL = 1

# =============================================================================
